package org.github.nikalaikina.manalyser.util

object GraphicUtil {

/*  def smooth(list: Iterable[Int]): Iterable[Int] = {
    list.zipWithIndex.map {
      case (item, 0) =>
        item
      case (item, list.size - 1) =>
        item
      case (item, x) =>

        val nextItem = list(x + 1)
        val prevItem = list(x - 1)
        item * (X-x(i+1)) *(x-x(i+2))/(-2h^2) * nextItem *(X-xi)*(x-x(i+2))/(-h^2)* Y(i+2) * (X-Xi)*(x-x(i+2))/(2h^2)
    }
  }*/

}
